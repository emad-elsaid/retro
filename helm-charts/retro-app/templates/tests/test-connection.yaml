apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "retro-app.fullname" . }}-test-connection"
  labels:
    {{- include "retro-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "retro-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
